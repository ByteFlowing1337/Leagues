<UserControl x:Class="Leagues.MatchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Leagues.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:MatchViewModel}"
             MinHeight="300" MinWidth="400">

    <Grid Margin="16">
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Search"
                      Command="{Binding ShowSearchViewCommand}">
                <MenuItem.Icon>
                    <materialDesign:PackIcon Kind="Search" />
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="Stats"
                      Command="{Binding ShowStatsViewCommand}">
                <MenuItem.Icon>
                    <materialDesign:PackIcon Kind="History" />
                </MenuItem.Icon>
            </MenuItem>
        </Menu>
        <ContentControl Content="{Binding CurrentViewModel}"
                        Margin="24" />
    </Grid>
</UserControl>